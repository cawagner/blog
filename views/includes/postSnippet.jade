article.post.preview
  header
    h1
      a(href=post.url)
        = post.title
    time(pubdate) #{post.date.getMonth()+1}.#{post.date.getDate()}.#{post.date.getFullYear()}
  != post.preview
  footer
    - each tag in post.tags
      a(href=tagUrl(tag))
        span.label.label-info #{tag}
